groups:
  - name: ml-drift-alerts
    rules:
      - alert: ModelConfidenceDrift
        expr: prediction_confidence_score < 0.607
        for: 1m
        labels:
          severity: critical
        annotations:
          summary: "Model confidence drift detected"
          description: "Prediction confidence score dropped below assigned threshold 0.607."
